import NotFoundMessage from "@/components/common/NotFound"
const NotFound = () => {
  return (
    <section className="flex min-h-screen flex-col items-center justify-center gap-4 p-10">
      <NotFoundMessage />
    </section>
  );
};

export default NotFound;
